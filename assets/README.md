# Assets

Place `icon.png` (app icon, preferably 256x256 or larger) and `tray-icon.png` (16x16 or 32x32) here.

The application falls back to a minimal embedded icon if these files are missing.

You can generate simple placeholders or replace with custom Xenogears-inspired art.
